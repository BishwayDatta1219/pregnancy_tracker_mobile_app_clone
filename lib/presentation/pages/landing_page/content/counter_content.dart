import 'package:flutter/cupertino.dart';

class CounterContent extends StatelessWidget {
  const CounterContent({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("this is counter content"),);
  }
}
